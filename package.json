{
  "name": "goggles",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["server", "client", "shared"],
  "scripts": {
    "dev": "npm run build -w shared && npm-run-all --parallel dev:shared dev:server dev:client",
    "dev:shared": "npm run dev -w shared",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
